# Copy this file to `.env` and set your Gemini API key before building.
# WARNING: Frontend environment variables are embedded into the client bundle at build time and are publicly visible.
# Use a server-side proxy for secure usage in production.

REACT_APP_GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE
